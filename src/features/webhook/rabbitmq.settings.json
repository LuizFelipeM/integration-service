{
    "webhook": {
        "queue": {
            "name": "webhook.commands",
            "durable": true,
            "bindings": [
                {
                    "exchange": "commands",
                    "routingKey": "webhook.commands"
                }
            ]
        }
    }
}